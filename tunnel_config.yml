tunnel: f2881b4d-72d0-466b-ae6c-44cbc0ba3ca0
# We rely on Vite Proxy to route /api (including /api/media) to the backend container.
# This simplifies the tunnel configuration and ensures consistency.

ingress:
  - hostname: dev-qrems.raylei-lab.com
    service: http://frontend:80
  
  - service: http_status:404